# include "DataStructure.h"

using namespace std;

tableInfo::tableInfo(string na, string pa, int tu) : name(na), path(pa), nuTuples(tu) { };

attsInfo::attsInfo(string na, Type ty, int di) : name(na), type(ty), disVal(di) { };

